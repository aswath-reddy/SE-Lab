1. Launch an EC2 Instance (Ubuntu)

Steps:

Go to AWS Management Console → EC2.
Click Launch Instance.
Enter instance name → “MyUbuntuAppServer”.
Choose Ubuntu Server 22.04 LTS (Free Tier).
Choose Instance type → t3.micro (Free Tier eligible).
Create a Key Pair → mykey.pem (download it).
Configure Security Group:
Allow SSH (port 22) — your IP.
Allow HTTP (port 80) — anywhere.
If app runs on another port (e.g., 3000, 8080), allow that too.
Launch the instance.

Click on Connect -> SSH Client & copy the ssh command


2. Install Docker and Required Software

SSH into EC2
  ssh -i "mykey.pem" ubuntu@54.123.98.11
Update packages
  sudo apt update && sudo apt upgrade -y
Install Docker
  sudo apt install docker.io -y
Install Git
  sudo apt install git
Install nano
  sudo apt install nano

3. Clone Your GitHub Application
git clone https://github.com/aswath-reddy/AWSExample.git
ls
cd AWSExample

4. Write the Dockerfile
nano Dockerfile
  FROM nginx:alpine
  COPY . /usr/share/nginx/html

5. Build the Docker Image & Run It
sudo docker build -t myapp-image .
sudo docker run -d -p 80:80 myapp-image
sudo docker ps

6. Demonstrate Deployment Using Public IPv4 address
http://Public Ipv4 address:80
Ex: http://54.123.98.11:80
